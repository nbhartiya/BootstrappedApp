<!DOCTYPE html>
<html>
<head>
  <title>Bootstrap - <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if user_signed_in? %>
<p> You're logged in as
  <%= current_user.email %>.
  <%= link_to "Sign out", destroy_user_session_path, :method => :delete, :confirm => "Are you sure you want to sign out?" %> </p>
<% end %>

<%= yield %>

</body>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
</html>
